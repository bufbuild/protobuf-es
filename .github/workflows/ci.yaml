name: ci
on:
  push:
    branches: [main, v2]
    tags: ['v*']
  pull_request:
    branches: [main, v2]
  workflow_dispatch:
jobs:
#  ci:
#    runs-on: ubuntu-20.04
#    strategy:
#      matrix:
#        node-version: [18, 20]
#    steps:
#      - uses: actions/setup-node@v4
#        with:
#          node-version: ${{ matrix.node-version }}
#      - name: checkout
#        uses: actions/checkout@v4
#      - name: Cache
#        uses: actions/cache@v4
#        with:
#          path: |
#            ~/.tmp
#            .tmp
#          key: ${{ runner.os }}-protobuf-es-ci-${{ hashFiles('Makefile') }}
#          restore-keys: |
#            ${{ runner.os }}-protobuf-es-ci-
#      - name: make
#        run: make ci
  test:
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        node-version: [18, 20]
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - name: checkout
        uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.tmp
            .tmp
          key: ${{ runner.os }}-protobuf-es-test-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-protobuf-es-test-
      - name: NPM Install
        run: npm ci
      - name: Run all tests except conformance and TypeScript compat
        run: npx turbo run test -F '!./packages/protobuf-conformance' -F '!./packages/typescript-compat/*'
  conformance:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.tmp
            .tmp
          key: ${{ runner.os }}-protobuf-es-conformance-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-protobuf-es-conformance-
      - name: NPM Install
        run: npm ci
      - name: Run conformance tests
        run: npx turbo run test -F './packages/protobuf-conformance'
  lint:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.tmp
            .tmp
          key: ${{ runner.os }}-protobuf-es-lint-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-protobuf-es-lint-
      - name: NPM Install
        run: npm ci
      - name: lint
        run: npx turbo run lint
  attw:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.tmp
            .tmp
          key: ${{ runner.os }}-protobuf-es-attw-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-protobuf-es-attw-
      - name: NPM Install
        run: npm ci
      - name: Are the types wrong
        run: npx turbo run attw
  typescript-compat:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.tmp
            .tmp
          key: ${{ runner.os }}-protobuf-es-typescript-compat-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-protobuf-es-typescript-compat-
      - name: NPM Install
        run: npm ci
      - name: Test typescript-compat
        run: npx turbo run test -F './packages/typescript-compat/*'
  license-header:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.tmp
            .tmp
          key: ${{ runner.os }}-protobuf-es-license-header-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-protobuf-es-license-header-
      - name: NPM Install
        run: npm ci
      - name: Update license headers
        run: npx turbo run license-header
      - name: Check changed files
        run: node scripts/gh-diffcheck.js
  format:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.tmp
            .tmp
          key: ${{ runner.os }}-protobuf-es-format-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-protobuf-es-format-
      - name: NPM Install
        run: npm ci
      - name: Format
        run: npx turbo run format
      - name: Check changed files
        run: node scripts/gh-diffcheck.js
  bundle-size:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.tmp
            .tmp
          key: ${{ runner.os }}-protobuf-es-bundle-size-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-protobuf-es-bundle-size-
      - name: NPM Install
        run: npm ci
      - name: Format
        run: npx turbo run bundle-size
      - name: Check changed files
        run: node scripts/gh-diffcheck.js
  bootstrap:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.tmp
            .tmp
          key: ${{ runner.os }}-protobuf-es-bootstrap-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-protobuf-es-bootstrap-
      - name: NPM Install
        run: npm ci
      - name: Bootstrap well-known types
        run: npx turbo run bootstrap:inject bootstrap:wkt
      - name: Check changed files
        run: node scripts/gh-diffcheck.js
