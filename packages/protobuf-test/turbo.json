{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "perf": {
      "dependsOn": ["^build", "generate"],
      "cache": false,
      "outputLogs": "new-only"
    },
    "profile": {
      "dependsOn": ["^build", "generate"],
      "cache": false,
      "outputLogs": "new-only"
    },
    "flamegraph": {
      "dependsOn": ["^build", "generate"],
      "cache": false,
      "outputLogs": "new-only"
    }
  }
}
