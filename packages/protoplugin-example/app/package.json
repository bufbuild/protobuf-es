{
  "name": "twirp-app",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "link": "npm link ../../protoplugin ../../protobuf ../../protoc-gen-es ../protoc-gen-twirp-es",
    "buf:generate": "npm run link && buf generate buf.build/bufbuild/eliza",
    "start": "npm run buf:generate && npm run build",
    "build": "npx tsc --noEmit && npx esbuild src/index.ts --serve=localhost:3000 --servedir=www --outdir=www --bundle --global-name=eliza"
  },
  "author": "Buf",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/bufbuild/protobuf-es.git",
    "directory": "packages/protoplugin-example"
  },
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "esbuild": "^0.15.10",
    "typescript": "^4.8.4"
  }
}
